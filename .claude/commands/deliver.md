---
description: Full implementation harness — GitHub issue to PR. Orchestrates grill, plan, TDD, code-review, spec-check (loop), commit, and PR.
---

Load and follow the `deliver` skill. Use `$ARGUMENTS` as the GitHub issue number or additional context.
